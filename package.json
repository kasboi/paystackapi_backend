{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for paystack api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend"
  },
  "author": "KAS",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "connect-history-api-fallback": "^2.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.1"
  }
}
